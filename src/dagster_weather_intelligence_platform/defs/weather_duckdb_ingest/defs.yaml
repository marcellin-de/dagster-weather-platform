type: dagster_weather_intelligence_platform.defs.weather_duckdb_ingest.partitioned_component.PartitionedDltLoadCollectionComponent

attributes:
  loads:
    - source: .loads.weather_source
      pipeline: .loads.weather_pipeline
      translation:
        group_name: weather_ingestion
        description: "Open-Meteo hourly weather loaded via dlt into DuckDB"
